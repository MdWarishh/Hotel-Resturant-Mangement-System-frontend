'use client'

import BookingDetailsPage from "@/app/(dashboard)/hotel-admin/bookings/[id]/page";


export default function CashierBookingDetails() {
  // This renders the exact same form, logic, and image upload
  // that you already built and fixed for the Admin.
  return <BookingDetailsPage />;
}