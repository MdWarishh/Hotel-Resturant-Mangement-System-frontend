'use client'

import CreateBookingPage from "@/app/(dashboard)/hotel-admin/bookings/create/page";

export default function CashierCreateBooking() {
  // This renders the exact same create booking form
  // that you already built for the Admin.
  return <CreateBookingPage />;
}